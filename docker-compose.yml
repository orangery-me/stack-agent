version: "3.8"

services:
  chat:
    image: orangery/stack-agent-api:latest
    container_name: ${APP_NAME:-stack-agent}-api
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - stack-network

networks:
  stack-network:
    external: true
